<h1>All Books</h1>

<% @books.each do |b| %>
  <div style="display:inline-block">
    <%= link_to book_path(b) do %>
      <% if b.image.present? %>
        <%= image_tag b.image.url(:small) %>
      <% end %>
      <div><%= b.title %></div>
    <% end %>
  </div>
<% end %>
